# Backend origin the browser calls. Read at BUILD time (Vite inlines it), so set it before
# `npm run build` in any deployed environment. No trailing slash needed.
VITE_API_BASE_URL=http://localhost:8000
