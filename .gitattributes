# Shell scripts run inside Linux containers: they must keep LF line endings even when checked
# out on Windows (core.autocrlf would otherwise turn them into CRLF and break "/bin/sh").
*.sh text eol=lf
